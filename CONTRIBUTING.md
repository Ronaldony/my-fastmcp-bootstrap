# 문서 작성·개정 지침

이 지침은 [FastMCP 부트스트랩 설계도](./fastmcp-bootstrap-blueprint.md)와 [README](./README.md)를 수정하는 사람과 AI에게 적용한다. 문서가 반복 개정되어도 특정 구축 사례의 기록으로 되돌아가지 않도록, 사용자 요구를 다음 원칙으로 유지한다.

## 1. 문서의 목적

**최소한의 시간·금전적 비용으로, 목적에 맞는 FastMCP를 빠르게 빌드업할 수 있는 범용 기능 설계도를 제공한다.**

재사용할 대상은 특정 프로젝트의 파일 묶음이나 실행 이력이 아니라 **기능 조합, 연결 규칙, 선택 기준, 구축 순서와 검증 방법**이다. 내용을 추가할 때는 독자의 구축 결정을 더 빠르고 정확하게 만드는지 먼저 판단한다.

## 2. 반드시 유지할 다섯 가지 원칙

| 원칙 | 작성·개정 기준 |
|---|---|
| **FastMCP 자체에 집중한다.** | 특정 프로젝트명, 사용자명, 실제 경로, 장비 식별자, 실제 연결 주소 등 환경 고유값을 본문에 넣지 않는다. FastMCP가 맡는 역할과 기능 조합을 설명한다. |
| **구현 목록보다 기능 설계를 설명한다.** | 실제 파일·폴더 구조, Python 버전, 설치 이력보다 핵심 기능, 규칙, 선택 부가 기능, FastMCP 중심 연결 구성도와 워크플로를 우선한다. |
| **미반영 항목의 제외 목록을 만들지 않는다.** | 과거에 무엇을 구현하지 않았는지, 어떤 사용 환경 때문에 기능을 제외했는지 나열하지 않는다. 현재 설계에 필요한 기능과 선택 조건을 설명한다. |
| **연결 대상은 일반적인 역할로 표현한다.** | 특정 제작 도구나 서비스 이름 대신 MCP 클라이언트, 서비스 MCP, 로컬 MCP, 외부 MCP, 서비스 내부 bridge 등의 표현을 쓴다. 어떤 transport로 연결하고 어떤 기능으로 도구 노출을 선별하는지 설명한다. |
| **부트스트랩에 필요한 내용만 남긴다.** | 모든 작업 내역이나 문제 해결 과정을 보존하려 하지 않는다. 빠른 구축에 필요한 결정·순서·검증만 남기고 중복 설명과 불필요한 단계를 줄인다. |

### 일반화할 때 정확성을 잃지 않는다

`ProxyProvider`, `Namespace`, `BM25SearchTransform`, `Visibility`, `search_tools`, `call_tool` 같은 **공식 기능·API 이름은 필요한 곳에 유지**한다. 일반화 대상은 환경 고유값이지, 기능을 식별하는 용어가 아니다.

저장소 안의 문서 파일명과 상대 링크도 탐색을 위해 사용한다. 이를 특정 구현의 파일 구조를 상세히 기술하는 것과 혼동하지 않는다.

미반영 기능 목록을 삭제하더라도 **연결 호환성, 인증 경계, 실제 호출 성공의 판정 조건처럼 설계 결정에 필요한 제약**까지 지우지는 않는다. 선택 부가 기능은 과거 적용 여부가 아니라 **어떤 요구에서 선택하며 무엇으로 검증하는지**를 기준으로 설명한다.

## 3. 내용을 추가하는 기준

새 기능 설명은 다음 네 질문에 답할 수 있을 때 포함한다.

| 질문 | 남길 내용 |
|---|---|
| 어떤 요구를 해결하는가? | 기능이 필요한 구체적인 상황 |
| 무엇을 조합하는가? | 사용할 FastMCP 기본 기능과 연결 관계 |
| 언제 선택하는가? | 기본 구성과 선택 확장의 구분, 추가 비용과 이점 |
| 무엇으로 확인하는가? | 실제 동작을 판단할 최소 검증 기준 |

기본 기능으로 같은 요구를 충족할 수 있으면 그 구성을 먼저 설명한다. 추가 코드나 의존성을 제안할 때는 필요한 이유와 더 단순한 대안과의 차이를 밝힌다.

효율은 문서 길이나 초기 도구 목록 크기만으로 판단하지 않는다. **구축 시간, 서비스 이용료, 추가 코드의 유지 비용, 검색·호출 왕복**을 함께 고려한다. 무료 여부나 성능 우위는 확인된 조건 없이 단정하지 않는다.

예시는 연결도, 역할 기반 자리표시자, 짧은 의사 흐름을 우선한다. 실행 코드나 구체적인 값이 꼭 필요할 때만 설명에 필요한 최소 범위로 제시하고, 검증하지 않은 예시를 실행 완료 사실처럼 쓰지 않는다.

## 4. 개정 절차

1. **변경 목적을 한 문장으로 정한다.** 어떤 구축 결정이나 검증이 개선되는지 명확히 한다.
2. **바뀌는 기술 내용을 확인한다.** 기능·API 동작을 바꾸는 수정은 공식 문서와 필요한 최소 검증에 근거한다. 적용 조건이 있는 동작을 모든 환경의 공통 규칙으로 확대하지 않는다.
3. **설계도와 안내를 구분해 수정한다.** 상세 기능 규칙은 설계도에, 개요와 진입 경로는 README에 둔다. 작성 원칙은 이 지침에서 관리한다.
4. **아래 점검표와 링크를 확인한다.** 절 제목을 변경했다면 README의 해당 절 링크도 함께 갱신한다.
5. **Git 커밋으로 변경을 남긴다.** 커밋 메시지에 변경 이유와 핵심 설계 영향을 짧게 기록한다. 문서 개정 이력을 특정 런타임 버전이나 구축 사례의 연혁으로 대체하지 않는다.

지침 자체의 변경은 본문 기능 설명의 수정과 구분하고, 변경 이유를 명시한다. 문서를 업데이트한다는 이유만으로 이 목적과 원칙을 암묵적으로 바꾸지 않는다.

## 5. 반영 전 점검표

- [ ] 특정 프로젝트·장비·사용자의 실제 이름, 경로, 연결 정보가 남아 있지 않다.
- [ ] 파일·버전·작업 이력보다 기능, 연결 규칙, 선택 기준과 구축 흐름이 중심이다.
- [ ] 미반영 항목을 나열하지 않으며, 필요한 제약과 선택 기능의 적용 조건은 명확하다.
- [ ] 특정 서비스명 없이도 연결 구성과 도구 노출·검색·호출 흐름을 이해할 수 있다.
- [ ] 새 내용이 구축 시간·비용을 줄이거나 검증의 정확성을 높이며, 중복된 절차를 늘리지 않는다.
- [ ] 변경한 기술 설명의 근거·검증 범위가 분명하고, README·설계도·상대 링크가 서로 일치한다.

**판단 기준:** 다른 서비스 MCP와 다른 실행환경을 가진 독자가, 과거 프로젝트를 알아야 한다는 전제 없이 이 문서만으로 필요한 FastMCP 구성을 선택하고 검증할 수 있어야 한다.
